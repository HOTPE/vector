#include <iostream>
#include <vector>
using namespace std;
int main()
{
	vector<int> v;
	int a, tmp;
	cin >> a;
	for (int i = 0; i < a; i++)
	{
		cin >> tmp;
		v.push_back(tmp);
	}
	int n;
	cin >> n;
	v.erase(v.begin() + n);
	for (int i = 0; i < v.size(); i++)
	{
		cout << v[i] << " ";
	}

	return 0;
}
