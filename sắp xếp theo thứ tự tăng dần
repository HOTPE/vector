#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main()
{
	vector<int> v;
	int a, tmp;
	cin >> a;
	for (int i = 0; i < a; i++)
	{
		cin >> tmp;
		v.push_back(tmp);
	}
	sort(v.begin(), v.end());
	for (int x : v)
		cout << x << " ";

	return 0;
}
